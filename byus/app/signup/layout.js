export const metadata = {
  title: 'Create Your Account — ByUs',
  description: 'Join ByUs as a creator or fan and get started in minutes.',
  alternates: { canonical: '/signup' },
};

export default function SignupLayout({ children }) {
  return children;
}
