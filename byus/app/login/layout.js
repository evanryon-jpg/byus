export const metadata = {
  title: 'Log In — ByUs',
  description: 'Log in to your ByUs creator or fan account.',
  alternates: { canonical: '/login' },
  robots: { index: false, follow: true },
};

export default function LoginLayout({ children }) {
  return children;
}
