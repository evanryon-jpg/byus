export const metadata = {
  title: 'Help Center — ByUs',
  description: 'Get answers about ByUs accounts, memberships, payments, posts, and creator tools.',
  alternates: { canonical: '/help' },
};

export default function HelpLayout({ children }) {
  return children;
}
